import { Component } from "solid-js";

const MainMenu: Component<{}> = (props) => {
  
  return <div>
    <div>Welcome to the HSR PVP Draft Tool</div>
    <button> Create game </button>
  </div>;
};

export default MainMenu;